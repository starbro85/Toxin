include ../icon/icon

mixin like({ value, active=false })
    button.like.js-like(
        data-active=active
        data-value=value
        )
        span.like__icon
            +icon()
        span.like__count.js-like__count