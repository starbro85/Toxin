mixin rate-button({ name, title, value, ariaLabel, checked=false, disabled=false })
    input(
        class = 'rate-button js-rate-button'
        type = 'radio'
        name = name
        value = value
        checked = checked
        disabled = disabled
        title = title
        aria-label = ariaLabel
    )