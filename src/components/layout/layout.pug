include ../header/header
include ../footer/footer

mixin layout(layout, lang)
    -
        const {
            header,
            footer
        } = layout;

    +header(header, lang)  

    if block
        block
    
    +footer(footer, lang)