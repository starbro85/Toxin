extends ../../components/ui-kit/ui-kit

include ../../components/cards/cards

block title
    | Start page
block style
    | .ui-kit { background-color: var(--dark-shade-5per); }


block main

    -const cards = require('./../../globals/mocks/cards.json');
    -const rooms = require('./../../globals/mocks/rooms.json');

    +cards({
        cards,
        rooms: [
            rooms[0],
            rooms[1]
        ]
    })