extends ../../globals/template

include ../../components/ui-kit-layout/ui-kit-layout
include ../../components/headers-and-footers/headers-and-footers

block title
    | Form elements

block content
    +ui-kit-layout({
        backgroundColor: 'grey'
    })

        -const headersAndFooters = require('./../../globals/mocks/headers-and-footers.json');
        -const user = require('./../../globals/mocks/user.json');

        +headers-and-footers({ headersAndFooters, user })