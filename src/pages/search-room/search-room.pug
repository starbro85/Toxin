extends ../../globals/template

include ../../components/layout/layout
include ../../components/search-room/search-room

block variables
    -
        const page = require('../../globals/mocks/search-room.ru.json');

        const { 
            lang,
            auth,
            user,
            title,
            description,
            searchRoom 
        } = page;

block heading
    | #{page.heading}

block content
    +layout(lang, auth, user)
        +search-room(searchRoom, lang)

